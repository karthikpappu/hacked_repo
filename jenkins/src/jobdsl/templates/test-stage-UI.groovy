// default job -- just an empty shell.

job {

	// remember, job configurations supplied from the 
	// engine itself can be injected here, such
	// as job name and the stitching to tie jobs together

}