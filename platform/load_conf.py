#

# aws account is "development" or "production"

# environment is any alpha-numeric string (no symbols allowed)

# Loads in the account conf and environment conf into a python dictionary (only keeping the stuff for this environment / account)
